# Materiality rules (PROMPT-09 §11)

Explicit table in `diff.mjs::classify` — reproduced in 05-change-model. Not graph-centrality-based. INFORMATIONAL exists so that publication metadata (cutoff moves) and rewording successors never inflate the material count; the /dossier/ overview says "žádná materiální změna" whenever the latest pair has none (currently true for r06→r07). Overrides go through canonical data + regeneration, never hand-edited output.
