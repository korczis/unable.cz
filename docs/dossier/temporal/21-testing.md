# Testing (PROMPT-09 §44)

Unit (`scripts/dossier-temporal.test.mjs`, 21 tests in `npm test`): valid-time precision (no fabricated day precision), semantic no-op + reorder immunity, field-diff semantics, the §44 fixture table (new shareholder, late discovery, corrected percentage, new primary source, blocked/recovered source, claim upgraded/contradicted, financial correction vs new period, supersession), plus non-vacuous invariants over the real generated data: snapshot chain+hashes, no-op guarantee against current canonical, per-pair reconciliation, publication-metadata-only last pair, gap-lifecycle links, time-axis discipline (observation never used as effective date; pre-case effective dates never WORLD_CHANGE), BLOCKED→plan wiring, notification determinism, publication safety.

Browser (`e2e/responsive.spec.mjs`, 92 tests): 12 routes × 7 viewports overflow/console matrix + interaction proofs — comparison URL cold-load + Back/Forward, change-detail time axes and revision links, snapshot comparisons + frozen objects.json served, overview snapshot id (sha256 in data attribute) + change block, graph change-mode banner with table alternative, timeline lane filter via ?lane= with per-event text tags, keyboard/scroll-lock checks. All green locally; suite is prod-targetable via PW_BASE_URL.
