# Adversarial temporal audit (PROMPT-09 §50)

Assumption: the implementation is misleading until proven otherwise. Checklist walked 2026-07-19 against the generated data, the built site and the tests. Severity: CRITICAL / HIGH / MEDIUM / LOW / OK.

| # | Attack | Verdict | Evidence / correction |
|---|---|---|---|
| 1 | Retrieval date shown as event date | **OK** | `effective_at` can only come from canonical valid-time fields (validator: effective without precision+source = error); retrievals live in the timeline's "zdroje" lane, text-tagged "pozorování evidence". Unit test "observation is never used as the effective date". |
| 2 | Filing date silently shown as effective date | **OK** | Financial facts carry no effective_at at all; period lives in the metric text; no day-precision is fabricated (parseValidTime test). |
| 3 | Late discovery presented as recent event | **OK** | Category rule + test: any effective date before the case's first cutoff is forced KNOWLEDGE_CHANGE; change pages carry an explicit warning sentence. Live example: CHG-0122 (ZenX-era shareholding, effective 2020, observed 2026-07-17). |
| 4 | Overwritten historical records | **OK** | Snapshots refuse divergent overwrite at write time; validator re-derives every hash and mirrors; unit test re-hashes real payloads. |
| 5 | False semantic changes caused by ordering / build timestamps | **OK** | id-lists sorted at extraction (reorder test); no wall-clock enters generated data; r06→r07 rebuild is a proven no-op; running temporal:build twice yields zero git diff. |
| 6 | Parser/extraction change presented as a company change | **MEDIUM, mitigated + documented** | If `extractObjects` semantics change, hashes of a re-extraction would differ — but old snapshots stay frozen (bootstrap refuses divergence) and the canonical-drift gate forces an explicit new snapshot; the changed-extraction pair would then need manual classification. No automated UPSTREAM/PARSER classifier exists (doc 11 marks §15 PARTIAL). Root cause: no second provider-run pair exists yet to build it against. |
| 7 | Stale relationship presented as current | **OK (as of cutoff)** | Currentness model separates evidence quality from currentness; ownership policy 30 d; as of 2026-07-18 nothing is past interval, and the four BLOCKED providers are queued top-priority. UI adds a client-side "po termínu k dnešnímu dni" marker so the static page cannot silently age. |
| 8 | Old snapshot page loading latest mutable JSON | **LOW, accepted** | Snapshot detail loads its own frozen manifest/objects; it also reads the changes manifest for the pair summary — that file is mutable but derived solely from immutable snapshots, so historical pair rows cannot drift (validator reconciles every pair on every build). |
| 9 | Public snapshot leaking internal revision / PII | **OK** | Frozen payloads scanned for local paths and rodné-číslo patterns at validation; bootstrapped payloads are exactly the states that were already publicly deployed. |
| 10 | Resolved gap silently removed | **HIGH — historical defect surfaced, by design** | The ledger *found real instances*: 8 open questions left publication (r03–r06) with no resolution record in canonical data (e.g. OQ-8e9b1fd2 — public contracts, actually answered by the SRC-20 Registr smluv query; OQ-e01750a5 — SRC-22 URL, answered by the r06 recovery). They are flagged `OBJECT_REMOVED_FROM_PUBLICATION` at MEDIUM materiality rather than reclassified, because inventing RQ records post-hoc would fabricate canonical history. Correction path: author real resolvedQuestions entries in a future canonical edit; the ledger will then link them. This is the temporal system doing its job on pre-temporal history. |
| 11 | Contradiction silently removed | **OK** | Contradiction count only ever grew (1→2→3); none removed in any pair. |
| 12 | Claim status changed without evidence | **OK for the real ledger** | All r04→r05 status upgrades carry the primary-register sources that caused them (source_ids from the claim's updated source list). No structural gate forces evidence on future status changes beyond claims-validate's own rules — noted as residual risk (LOW). |
| 13 | Current and historical conflated / ownership interpolation | **OK** | Historical roles (`until`) are NOT_APPLICABLE/HISTORICAL; ownership series shown verbatim as the sourced stepped string; no interpolation anywhere. |
| 14 | "NEW" badges without comparison basis | **OK** | No floating badges exist; every indicator names its snapshot pair. |
| 15 | Broken comparison links / mixed snapshot ids | **OK** | zola check (internal links), Playwright cold-load + Back test, validator's id-consistency checks. |
| 16 | Deployment claiming a different snapshot than production serves | **pending → see 24** | production-proof.mjs compares canonical/generated/built/deployed ids + sha256 and the id/hash rendered into the production page; §13-style retest recorded in the final report. |
| 17 | Timeline mixing acquisition dates with discovery dates | **OK** | Lanes + per-event text tags; Playwright lane test asserts every event in ?lane=zdroje is tagged "pozorování evidence". |
| 18 | Freshness laundering a BLOCKED screening | **OK (fixed during build)** | Found & fixed in-pass: ISIR's 200-OK landing page masked the failed lustrace (provider looked AVAILABLE). Corrected to most-recent-outcome-wins on full timestamps + sticky BLOCKED for records whose own status is BLOCKED; regression-tested. |
| 19 | Empty payloads for legacy string-shaped records | **OK (fixed during build)** | Found & fixed in-pass: old `financials.notFound` string entries extracted as all-null payloads, making distinct records hash-identical and masking NOT_FOUND→verified resolutions. Extraction normalizes strings; snapshots re-bootstrapped before first publication. |

Open residual risks, all documented: #6 (no provider-run pair diff engine), #10 (historical gap removals awaiting real resolution records in canonical data), #12 (no hard evidence-gate on future status changes). No CRITICAL or unmitigated HIGH defects remain.
